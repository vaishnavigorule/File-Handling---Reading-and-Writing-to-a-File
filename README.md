# File-Handling---Reading-and-Writing-to-a-File
 a program that reads data from a file, processes it, and writes the output to another file. Read a text file and process the content (e.g., count words, lines). Write the processed data (e.g., word count) to a new file. Handle file-related exceptions like FileNotFoundException.
